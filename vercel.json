{
  "version": 2,
  "buildCommand": "npm install --include=dev --prefix frontend && npm install --include=dev --prefix backend && npm run build --prefix frontend && npm run build --prefix backend",
  "outputDirectory": "frontend/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" },
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.*\\.(js|css|ico|png|svg|json))", "dest": "/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
